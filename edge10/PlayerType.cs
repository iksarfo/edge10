namespace edge10
{
    // This could be configured differently,
    // kept with the strategy implementation
    // and made available dynamically via web.config
    public enum PlayerType
    {
        Human,
        RandomComputer,
        TacticalComputer
    }
}